# PQSend v0.1.0-alpha.1

`v0.1.0-alpha.1` is the first experimental encrypted package alpha for PQSend.
PQSend is an experimental `.pqsend` encrypted package format with a Rust
reference CLI for private file delivery.

> [!WARNING]
> This alpha is experimental, unaudited, X25519-only, not
> post-quantum-secure, and not ready for sensitive real-world data. The
> `.pqsend` package format is unstable and may change incompatibly before
> `v1.0.0`.

## Included

- `.pqsend` encrypted package format documentation
- Rust reference CLI
- age-backed binary age v1 adapter for exactly one X25519 recipient
- fixed 20-byte `.pqsend` public envelope
- one-file `.pqsend` package creation and authenticated opening
- encrypted internal single-file manifest with the original filename hidden
  from public package metadata
- explicit recipient-file and identity-file workflow
- local contact workflow with recipient-bound fingerprint verification,
  default rejection of unverified contacts, and an explicit one-operation
  override
- safe public `inspect` of only the fixed public envelope and exact package
  length
- local human-readable security receipts
- security model, threat model, and compatibility/versioning documentation
- initial experimental valid and invalid `v0-alpha` test vectors
- security-sensitive tests for package, backend, extraction, inspection,
  contacts, receipts, and vector behavior

The public `inspect` command validates and displays only the public envelope and
total package size. It does not decrypt the package or reveal the encrypted
original filename.

## Documentation

- [Canonical package format](../FORMAT.md)
- [Security model](../SECURITY-MODEL.md)
- [Threat model](../THREAT-MODEL.md)
- [Contacts and local recipient trust](../CONTACTS.md)
- [Local security receipts](../RECEIPTS.md)
- [Compatibility and versioning](../COMPATIBILITY.md)
- [Initial test vectors](../../test-vectors/README.md)

## Security and privacy notes

- PQSend delegates recipient encryption and authenticated payload protection to
  the Rust `age` crate. It does not implement custom cryptographic primitives.
- The v0.1 file-size limit is 64 MiB (`67,108,864` bytes).
- The original filename is encrypted inside the package. The outer `.pqsend`
  filename is public transport metadata, so users who want to avoid filename
  leakage must not name the outer package after the original file.
- The public envelope reveals the format version, single-file mode, age/X25519
  backend, exact encrypted payload length, and approximate plaintext size.
- Package creation, key generation, and extraction refuse to overwrite
  existing files.
- Opening validates the complete authenticated package and rejects unsafe
  filenames before publishing plaintext output.
- Local security receipts are not signatures, proof of authorship, proof of
  delivery, or external proof.

## Known limitations

- This release is experimental and unaudited.
- The only backend is age v1 X25519. It is not post-quantum-secure and does not
  protect against harvest-now-decrypt-later or other quantum threats to
  X25519.
- The `.pqsend` package format is unstable, with no stable compatibility
  guarantee before `v1.0.0`.
- The local contact-store format is also experimental and has no pre-v1
  compatibility guarantee.
- Packages contain exactly one file for exactly one recipient.
- There is no folder or multi-file support, multiple-recipient support,
  signatures, password mode, GUI, networking, relay/server behavior, cloud
  sync, messaging, or chat.
- PQSend uses the Rust `age` crate rather than custom cryptography.
- Files, plaintext, encrypted payloads, and packages are buffered in memory at
  different stages; temporary and in-memory plaintext remnants cannot be
  guaranteed erased.
- Approximate plaintext size, package size, transfer timing, and outer package
  filename may leak.
- PQSend does not protect compromised endpoints, substituted or poorly
  protected keys, recipient plaintext disclosure, or all denial-of-service
  attacks.

Future capabilities are roadmap items only. They are not implemented
properties of this alpha. See the [roadmap](../../ROADMAP.md).

## Validation

Release documentation preparation requires:

```sh
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```
